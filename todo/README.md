# TODO

Copy `sample.env` to `.env` and update `DATABASE_URL` to your Postgres database

To start front-end

```bash
cd frontend
npm run dev
```

To start back-end
```bash
cd backend
npm run start
```

or using `nodemon`
```bash
cd backend
nodemon server.js
```
